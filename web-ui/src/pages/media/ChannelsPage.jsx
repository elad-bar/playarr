import React from 'react';
import { Box } from '@mui/material';
import ChannelsList from '../../components/channels/ChannelsList';

/**
 * Channels page showing live channels list.
 */
const ChannelsPage = () => (
  <Box>
    <ChannelsList />
  </Box>
);

export default ChannelsPage;

